# คู่มือการติดตั้งและใช้งานแอปพลิเคชันวิเคราะห์ฟิล์มกะหล่ำปลีม่วงอย่างถาวร
## (ใช้งานบนโทรศัพท์มือถือได้โดยตรงตลอดเวลา โดยไม่ต้องเปิดคอมพิวเตอร์)

ระบบเว็บแอปพลิเคชันนี้ได้รับการออกแบบด้วยสถาปัตยกรรม **PWA (Progressive Web App) + Hybrid Storage Engine** ทำให้สามารถติดตั้งเป็นแอปพลิเคชันลงบนหน้าจอโทรศัพท์มือถือ (ทั้ง iOS และ Android) และเปิดใช้งานได้ทุกที่ทุกเวลาโดยไม่ต้องเปิดคอมพิวเตอร์

---

## 📱 วิธีที่ 1: ติดตั้งเป็นแอปลงบนมือถือ (เปิดได้ถาวรโดยไม่ต้องใช้คอมพิวเตอร์)

เมื่อเว็บไซต์ถูกนำขึ้นออนไลน์ (ดูวิธีนำขึ้นโฮสต์ฟรีในหัวข้อถัดไป):

### สำหรับโทรศัพท์ระบบ Android (ผ่าน Google Chrome):
1. เปิดลิงก์เว็บไซต์ของท่านใน **Google Chrome** บนโทรศัพท์มือถือ
2. ระบบจะมีปุ่มสีม่วง **"ติดตั้งแอป"** ปรากฏขึ้นที่มุมขวาบน หรือกดที่ **เมนู 3 จุด (⋮)** ที่มุมขวาบนของเบราว์เซอร์ Chrome
3. เลือกเมนู **"ติดตั้งแอป" (Install app)** หรือ **"เพิ่มลงในหน้าจอหลัก" (Add to Home screen)**
4. ยืนยันการติดตั้ง ไอคอน **Cabbage Film Color Lab** จะปรากฏบนหน้าจอหลักของโทรศัพท์มือถือเหมือนแอปทั่วไป
5. สามารถกดเปิดใช้งาน ถ่ายภาพ สแกนฟิล์ม และดูประวัติได้ทันที โดยทำงานได้แบบ **Offline 100%** ผ่าน LocalStorage ภายในเครื่อง

### สำหรับโทรศัพท์ iPhone / iPad (ผ่าน Safari):
1. เปิดลิงก์เว็บไซต์ของท่านใน **Safari** บนโทรศัพท์มือถือ
2. กดปุ่ม **แชร์ (Share)** ที่แถบด้านล่าง (ไอคอนสี่เหลี่ยมที่มีลูกศรชี้ขึ้น `⎋`)
3. เลื่อนลงมาแล้วเลือก **"เพิ่มไปยังหน้าจอโฮม" (Add to Home Screen)**
4. ตั้งชื่อแล้วกด **"เพิ่ม" (Add)**
5. ไอคอนแอปจะไปอยู่ที่หน้าจอโฮมของ iPhone สามารถแตะเปิดใช้งานเต็มจอแบบ Native App ได้ทันที

---

## 🌐 วิธีที่ 2: การนำเว็บไซต์ขึ้น Cloud Hosting ฟรี (เปิดออนไลน์ตลอด 24 ชั่วโมง)

เพื่อให้ทีมงานทุกคนสามารถเข้าถึงลิงก์เดียวกันได้จากทุกที่ และซิงค์ฐานข้อมูล MySQL ออนไลน์อย่างถาวร:

### ทางเลือกโฮสติ้งฟรีแนะนำ:
1. **InfinityFree** (https://www.infinityfree.com) — ฟรีถาวร 100%, มี PHP 8.2 + MySQL + phpMyAdmin ไม่จำกัดแบนด์วิดท์
2. **Alwaysdata** (https://www.alwaysdata.com) — ฟรี มี SSH/FTP, PHP และ MySQL ใช้งานง่ายและเสถียรมาก

### ขั้นตอนการนำขึ้นระบบ:
1. **สร้าง Database บนโฮสต์**:
   - เข้าสู่ Control Panel ของโฮสติ้ง (เช่น cPanel / vPanel)
   - ไปที่เมนู **MySQL Databases** สร้างฐานข้อมูลใหม่ เช่น `film_db`
   - จดบันทึกค่า: `DB_HOST`, `DB_NAME`, `DB_USER`, `DB_PASS`
2. **นำเข้าโครงสร้างตาราง (Import SQL)**:
   - เปิดเข้า **phpMyAdmin** ของโฮสติ้ง
   - เลือกฐานข้อมูลที่เพิ่งสร้าง
   - ไปที่แท็บ **Import (นำเข้า)** เลือกไฟล์ `database/schema.sql` แล้วกด Go
3. **อัปโหลดไฟล์ขึ้นเซิร์ฟเวอร์**:
   - เข้า **File Manager** หรือใช้โปรแกรม FTP (เช่น FileZilla)
   - อัปโหลดไฟล์และโฟลเดอร์ทั้งหมดนี้เข้าไปในโฟลเดอร์ `htdocs` หรือ `public_html` ของโฮสติ้ง
4. **แก้ไขการตั้งค่าฐานข้อมูล**:
   - เปิดไฟล์ `api/db.php` แล้วใส่ข้อมูล Host, Username, Password, Database Name ตามที่โฮสติ้งกำหนด:
   ```php
   $db_host = 'sqlXXX.infinityfree.com'; // โฮสต์ของท่าน
   $db_name = 'if0_XXXX_film_db';
   $db_user = 'if0_XXXX';
   $db_pass = 'รหัสผ่านฐานข้อมูลของท่าน';
   ```
5. **เข้าใช้งาน**:
   - เปิด URL โดเมนฟรีที่โฮสติ้งให้มา เช่น `https://your-domain.epizy.com` บนมือถือ
   - เว็บไซต์จะเชื่อมต่อ MySQL แบบออนไลน์ และสามารถติดตั้งเป็น PWA ตามวิธีที่ 1 ได้ทันที

---

## 💻 วิธีที่ 3: การรันในเครื่องคอมพิวเตอร์ด้วย XAMPP (สำหรับพัฒนาในเครื่อง)

หากต้องการเปิดทดสอบในเครื่องคอมพิวเตอร์:
1. ติดตั้งโปรแกรม **XAMPP**
2. คัดลอกโฟลเดอร์โปรเจกต์นี้ทั้งหมด ไปวางไว้ที่:
   ```
   C:\xampp\htdocs\film-app
   ```
3. เปิดโปรแกรม **XAMPP Control Panel** แล้วกดปุ่ม **Start** ที่โมดูล **Apache** และ **MySQL**
4. เปิดเบราว์เซอร์ไปที่ `http://localhost/phpmyadmin`
   - สร้างฐานข้อมูลชื่อ `film_db`
   - นำเข้าไฟล์ `database/schema.sql`
5. เปิดเข้าใช้งานระบบได้ที่:
   ```
   http://localhost/film-app/
   ```
   *(หมายเหตุ: การเปิดกล้องมือถือผ่านเครือข่าย Wi-Fi เดียวกัน ต้องเปิดผ่าน `localhost` หรือ HTTPS ตามมาตรฐานความปลอดภัยของเบราว์เซอร์ หรือใช้วิธีปุ่ม "อัปโหลดภาพ" เมื่อเปิดผ่าน IP เครือข่าย)*

---

## 🔬 การปรับจูนเกณฑ์การทดลอง (Calibration)

หากต้องการปรับแก้ค่ามาตรฐานสีตั้งต้นหรือเกณฑ์การตัดระดับ:
1. เข้าไปที่แท็บ **"ตั้งค่าเกณฑ์" (Settings)** ในแอปพลิเคชัน
2. สามารถเปลี่ยนค่า:
   - **Baseline Red, Green, Blue**: ค่าสีม่วงของฟิล์มกะหล่ำปลีม่วงสดก่อนการทดลอง
   - **เกณฑ์ตัดระดับต่ำ (Delta E Low)**: ค่าความต่างสีที่ไม่เกินเกณฑ์นี้จะจัดเป็นระดับต่ำ
   - **เกณฑ์ตัดระดับปานกลาง (Delta E Medium)**: ค่าความต่างสีตั้งแต่ค่านี้ขึ้นไปจะจัดเป็นระดับสูง
3. กด **"บันทึกการตั้งค่า"** ระบบจะจำค่าใหม่ไว้ในเครื่องทันที
